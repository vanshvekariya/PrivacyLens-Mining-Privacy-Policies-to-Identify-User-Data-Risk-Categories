# PrivacyLens source scripts

The complete setup, reproduction order, output inventory, prototype command,
and test instructions are maintained in the repository-level
[`README.md`](../README.md).

Scripts in this directory are designed to be run from the repository root so
that all paths resolve consistently.
